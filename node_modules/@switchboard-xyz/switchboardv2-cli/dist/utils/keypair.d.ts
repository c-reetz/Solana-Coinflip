import { Keypair, PublicKey } from "@solana/web3.js";
export interface fromLocalKeypair {
    localPath: string;
}
export interface fromGoogleSecret {
    secretPath: string;
}
export declare type KeypairDescriptor = string | fromLocalKeypair | fromGoogleSecret;
export interface IKeypairDescriptor {
    publicKey: PublicKey;
    localPath?: string;
    secretPath?: string;
}
export declare const loadKeypairFs: (keypairPath: string) => Keypair;
export declare const loadGoogleSecretKeypair: (secretPath: string) => Promise<Keypair>;
export declare const loadKeypair: (keypairPath: KeypairDescriptor) => Promise<Keypair>;
/** Loads a keypair with a descriptor to find and retrieve on-command */
export declare const loadKeypairWithDescriptor: (keypairPath: KeypairDescriptor) => Promise<[Keypair, IKeypairDescriptor]>;
