import { CliConfig } from "../../config";
import { FsProvider } from "./FsProvider";
import { LogProvider } from "./logging";
export interface CommandContext {
    logger: LogProvider;
    fs?: FsProvider;
    config: CliConfig;
}
export declare const DEFAULT_CONTEXT: CommandContext;
