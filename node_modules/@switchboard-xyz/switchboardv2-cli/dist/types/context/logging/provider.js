"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.DEFAULT_LOGGER = exports.CONSOLE_ONLY_LOGGER = exports.LogProvider = void 0;
const winston_1 = require("./winston");
class LogProvider {
    constructor(parameters) {
        this.silent = false;
        this.verbose = false;
        this.logger = new winston_1.WinstonLogger(parameters);
        this.silent = parameters.silent;
        this.verbose = parameters.verbose;
    }
    log(message) {
        this.logger.info(message);
    }
    info(message) {
        this.logger.info(message);
    }
    warn(message) {
        this.logger.warn(message);
    }
    error(message) {
        this.logger.error(message);
    }
    debug(message) {
        this.logger.debug(message);
    }
}
exports.LogProvider = LogProvider;
exports.CONSOLE_ONLY_LOGGER = {
    console: { level: "info" },
};
exports.DEFAULT_LOGGER = new LogProvider(exports.CONSOLE_ONLY_LOGGER);
