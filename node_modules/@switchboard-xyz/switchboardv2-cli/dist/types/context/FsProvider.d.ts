import { Keypair } from "@solana/web3.js";
import { LogProvider } from "./logging";
export declare class FsProvider {
    dataDir: string;
    keypairPath: string;
    logger: LogProvider;
    constructor(dataDirectory: string, logger?: LogProvider);
    saveKeypair(keypair: Keypair): void;
    loadKeypair(publicKey: string): Keypair | undefined;
    saveAccount(file: string, account: any): void;
}
