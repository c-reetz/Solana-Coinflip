import { Command } from "@oclif/core";
import * as anchor from "@project-serum/anchor";
import { PublicKey } from "@solana/web3.js";
import { SwitchboardAccountType } from "@switchboard-xyz/sbv2-utils";
import { CliConfig } from "./config";
import { CommandContext } from "./types/context/context";
import { LogProvider } from "./types/context/logging";
export interface ClusterConfigs {
    devnet: anchor.Program;
    mainnet: anchor.Program;
}
declare abstract class PrintBaseCommand extends Command {
    static flags: {
        help: import("@oclif/core/lib/interfaces").BooleanFlag<void>;
        verbose: import("@oclif/core/lib/interfaces").BooleanFlag<boolean>;
    };
    cliConfig: CliConfig;
    logger: LogProvider;
    context: CommandContext;
    clusters: ClusterConfigs;
    init(): Promise<void>;
    private printAccount;
    printDevnetAccount(publicKey: PublicKey): Promise<SwitchboardAccountType>;
    printMainnetAccount(publicKey: PublicKey): Promise<SwitchboardAccountType>;
    catch(error: any, message?: string): Promise<void>;
}
export default PrintBaseCommand;
